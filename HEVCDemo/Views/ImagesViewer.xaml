<UserControl x:Class="HEVCDemo.Views.ImagesViewer"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks" xmlns:views="clr-namespace:HEVCDemo.Views"
             prism:ViewModelLocator.AutoWireViewModel="True">
    <Grid Margin="0" Width="1920">
        <Grid.RowDefinitions>
            <RowDefinition Height="1080"/>
        </Grid.RowDefinitions>
        
        <!--Image-->
        <Border Grid.Row="0" BorderThickness="2" BorderBrush="Black" Height="1080" Width="1920">
            <Image Source="{Binding CurrentImage}" />
        </Border>
        <!--CanvasTest is overlay-->
        <views:CanvasTest Grid.Row="0" Height="1080" Width="1920" Margin="0"/>
        <!--Controls-->
        <Grid Grid.Row="0" Height="1920" Width="1920">
        <Grid.RowDefinitions>
            <RowDefinition Height="38"/>
            <RowDefinition Height="720"/>
            <RowDefinition Height="38"/>
        </Grid.RowDefinitions>
            
        <Button Command="{Binding SelectVideoClick}" Width="100" HorizontalAlignment="Left" Margin="4">Select Video</Button>
        
        <Grid Grid.Row="2" HorizontalAlignment="Center">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Margin="4" Command="{Binding BackwardClick}">
                <iconPacks:PackIconModern Kind="NavigatePrevious"
                                 VerticalAlignment="Center"
                                 HorizontalAlignment="Center" />
            </Button>
            <Button Grid.Column="1" Margin="4"  Command="{Binding ForwardClick}">
                <iconPacks:PackIconModern Kind="NavigateNext"
                                 VerticalAlignment="Center"
                                 HorizontalAlignment="Center" />
            </Button>
        </Grid>
    </Grid>
    </Grid>
    <UserControl.InputBindings>
        <KeyBinding Key="Left" Command="{Binding BackwardClick}"/>
        <KeyBinding Key="Right" Command="{Binding ForwardClick}"/>
    </UserControl.InputBindings>
</UserControl>
